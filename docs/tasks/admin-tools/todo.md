# Todo — Admin Tools (Phase 5)

- [x] 1. AdminCoverageRepository + listAdminCoverage + addAdminCoverage + removeAdminCoverage + routes → QA gate A
- [x] 2. PartnerWorkspace reviewNote migration + workspace repo methods + listAdminWorkspaces + approveWorkspace + rejectWorkspace + routes → QA gate B
- [x] 3. AuditRepository listLogs + listAuditLogs use case + route → QA gate C
- [x] 4. Swagger docs + lint + build + roadmap update → Final gate
