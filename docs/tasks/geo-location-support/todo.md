# Todo — Geo/Location Support

- [x] 1. Add `listStates(countryId?)` and `listCities(stateId?)` to `GeoPlaceRepository`
- [x] 2. Implement `list-states.use-case.ts` and `list-cities.use-case.ts`; export from `use-cases/index.ts`
- [x] 3. `GET /api/geo/states` and `GET /api/geo/cities` routes + Swagger docs → QA gate
