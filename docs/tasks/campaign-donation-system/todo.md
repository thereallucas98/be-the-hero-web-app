# Todo — Campaign & Donation System

- [x] 1. CampaignRepository + createCampaign use case + schemas + POST /workspaces/:id/campaigns → QA gate A
- [x] 2. listWorkspaceCampaigns + GET /workspaces/:id/campaigns + getCampaignById + GET /campaigns/:id → QA gate B
- [x] 3. updateCampaign + PATCH /campaigns/:id + submitCampaignForReview + POST /campaigns/:id/submit-for-review → QA gate C
- [x] 4. CampaignDocumentRepository + addCampaignDocument + removeCampaignDocument + document routes → QA gate D
- [x] 5. listAdminCampaigns + approveCampaign + rejectCampaign + admin campaign routes → QA gate E
- [x] 6. DonationRepository + registerDonation + listCampaignDonations + schemas + routes → QA gate F
- [x] 7. listAdminDonations + approveDonation + rejectDonation + admin donation routes → QA gate G
- [x] 8. Swagger docs + export use cases + lint + build + update roadmap → Final gate
